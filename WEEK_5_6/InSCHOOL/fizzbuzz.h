#include <cstdio>
#include < cstring>
#include "fizzbuzz.h"

static char output[16];

char *fizz_buzz(int num)
{
    if (num < 1)
    {
        std::strcpy(output, " ");
    }
    else if
    {
        std::strcpy(output, " ");
    }
    else
    {
        std::sprintf(output, "%d", num);
    }

    return output;
}